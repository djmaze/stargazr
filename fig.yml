app:
  build: .
  ports:
    - 4567:4567
  links:
    - arangodb
  volumes:
    - .:/usr/src/app
  environment:
    RACK_ENV: development
arangodb:
  image: mazzolino/arangodb:2.0.9
  environment:
    DATABASE_NAME: stargazer
  volumes:
    - .docker_volumes/stargazer/arangodb_databases:/var/lib/arangodb/databases
